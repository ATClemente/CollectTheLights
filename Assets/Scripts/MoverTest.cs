using UnityEngine;
using System.Collections;

public class MoverTest : MonoBehaviour {


	private Vector3 maxUp;


	// Use this for initialization
	void Start () {


//		Vector3 move = transform.up * Time.deltaTime;
//		transform.position += move;

//		maxUp = new Vector3 (transform.position.x, transform.position.y + 0.5f, transform.position.z);

		//transform.position = Vector3.MoveTowards (transform.position, maxUp, 0.5f * Time.deltaTime);
	
	}
	
	// Update is called once per frame
	void Update () {
	//	transform.position = Vector3.MoveTowards (transform.position, maxUp, 0.5f * Time.deltaTime);
	
	}
}
